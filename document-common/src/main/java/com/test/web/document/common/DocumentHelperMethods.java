package com.test.web.document.common;

// Common helper methods that can be inherited by document implementations
// so that can share between Long and OO model
public interface DocumentHelperMethods {

	// Each must implement helper method for
}
